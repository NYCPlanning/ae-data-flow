services:
  db:
    extends:
      file: common-services.yml
      service: postgis
    networks:
      - data
  minio:
    extends:
      file: common-services.yml
      service: minio
networks:
  data:
    name: ae-zoning-api_data
    external: true
